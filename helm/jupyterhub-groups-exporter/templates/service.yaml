apiVersion: v1
kind: Service
metadata:
  name: {{ include "jupyterhub-groups-exporter.fullname" . }}
  labels:
    {{- include "jupyterhub-groups-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jupyterhub-groups-exporter.selectorLabels" . | nindent 4 }}
